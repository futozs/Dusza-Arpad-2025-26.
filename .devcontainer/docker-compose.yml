version: '3.9'

services:
  # Next.js alkalmazás
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: dusza-app
    restart: unless-stopped
    env_file:
      - ../.env

    network_mode: host
